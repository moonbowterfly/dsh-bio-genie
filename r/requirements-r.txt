# dsh-bio R 环境依赖清单（R 引导器读取本文件安装）。
# 保持精简：只列核心包，传递依赖由 BiocManager/install.packages 自动解析。
# 许可证见 THIRD_PARTY_NOTICES.md（本清单本身不复制任何源码，仅包名事实）。

# ---- CRAN ----
BiocManager
jsonlite
dplyr
tibble
readr
ggplot2

# ---- Bioconductor 3.23（对应 R 4.6）----
BiocGenerics
S4Vectors
IRanges
GenomicRanges
Biostrings
SummarizedExperiment
DESeq2
edgeR
limma
# 注：clusterProfiler 已移除——其依赖 GO.db 在 Bioc 3.23 无 Windows 二进制（上游缺口），
# Windows 装不上。R 侧富集用 fgsea（GSEA）+ Python bio_enrichr（ORA）即可覆盖。
fgsea
phyloseq
ggtree
ComplexHeatmap
# ---- Tier 1 轻量增强包（2026-08-18 新增，CRAN 二进制安全）----
msigdbr
Rtsne
